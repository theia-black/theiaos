# TheiaOS Podman environment
# Copy to theiaos.podman.env.local and set THEIAOS_GATEWAY_TOKEN (or use -e when running).
# This file can be used with:
#   THEIAOS_PODMAN_ENV=/path/to/theiaos.podman.env ./scripts/run-theiaos-podman.sh launch

# Required: gateway auth token. Generate with: openssl rand -hex 32
# Set this before running the container (or use run-theiaos-podman.sh which can generate it).
THEIAOS_GATEWAY_TOKEN=

# Optional: web provider (leave empty to skip)
# CLAUDE_AI_SESSION_KEY=
# CLAUDE_WEB_SESSION_KEY=
# CLAUDE_WEB_COOKIE=

# Host port mapping (defaults; override if needed)
THEIAOS_PODMAN_GATEWAY_HOST_PORT=18789
THEIAOS_PODMAN_BRIDGE_HOST_PORT=18790

# Gateway bind (used by the launch script)
THEIAOS_GATEWAY_BIND=lan

# Optional: LLM provider API keys (for zero cost use Ollama locally or Groq free tier)
# OLLAMA_API_KEY=ollama-local
# GROQ_API_KEY=
